{
  "name": "feature-email-server",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/feature/email/server/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/feature/email/server/jest.config.ts"
      }
    },
    "test:integration": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}/integration"],
      "options": {
        "jestConfig": "libs/feature/email/server/jest.integration.config.ts",
        "passWithNoTests": false
      }
    },
    "test:all": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run feature-email-server:test", "nx run feature-email-server:test:integration"],
        "parallel": false
      }
    }
  }
}
